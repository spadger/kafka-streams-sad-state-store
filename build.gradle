plugins {
    id 'java'
}

allprojects {
    sourceCompatibility = 1.8

    repositories {
        mavenCentral()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.8'
    distributionType = Wrapper.DistributionType.ALL
}